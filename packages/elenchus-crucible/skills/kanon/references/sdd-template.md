# SDD Template (T8) — per IEEE 1016-2009

Rule: one viewpoint governs each view; every element carries rationale. RFC Alternatives Considered seed viewpoints/rationale (SDD owns wording). Rationale-less diagrams fail. Every REQ-### maps to >=1 element; no ORPHAN.

## 1. SDD identification + references + definitions

## 2. Viewpoints (each: view + Rationale slot)

### 2.1 Context viewpoint
View: system boundary, external actors, scope.
Rationale:

### 2.2 Composition viewpoint
View: decomposition into components/subsystems.
Rationale:

### 2.3 Logical viewpoint
View: static structure (classes/modules, responsibilities).
Rationale:

### 2.4 Dependency viewpoint
View: inter-component dependencies, layering.
Rationale:

### 2.5 Information viewpoint
View: data design, dictionary, persistence.
Rationale:

### 2.6 Interface viewpoint
View: external + internal interfaces, contracts.
Rationale:

### 2.7 Interaction viewpoint
View: dynamic behavior, key scenarios/sequences.
Rationale:

### 2.8 Resource viewpoint
View: runtime resources, deployment, capacity.
Rationale:

## 3. Requirements traceability matrix

| REQ-### | Element(s) | Viewpoint |
|---|---|---|
| REQ-001 | <element> | <one of §2.1–2.8> |
