# Test Plan Template (IEEE 829 a–p)
<!-- Fill each numbered slot. Keep order; extras only before §16 Approvals. -->
## 1. Test Plan Identifier
`<id> / version <v> / date <yyyy-mm-dd>`
## 2. References
`<SRS/SDD/REQ ids>`
## 3. Introduction
`<objective, scope summary>`
## 4. Test items
`<builds, modules, versions under test>`
## 5. Software risk issues
`<link RSK-### ids>`
## 6. Features to be tested
`<REQ-### list + TST-### ids>`
## 7. features-NOT-tested
`derived-appendix: from V1-§5` — copy each kill criterion verbatim, one row: `<criterion> | reason: <out-of-scope|unfalsifiable-here|deferred>`.
## 8. Approach
`<levels: unit/integration/system/acceptance; techniques>`
## 9. Item pass/fail criteria
- pass criterion: `<quantified, e.g. ≥98% TST pass, 0 sev-1 open>`
- fail criterion: `<quantified, e.g. >2% TST fail OR any sev-1 open>`
## 10. Suspension criteria and resumption requirements
- suspension criterion: `<e.g. env down >4h OR blocker defect halts >50% cases>`
- resumption requirement: `<e.g. env restored + smoke suite 100% pass>`
## 11. Test deliverables
`<plan, cases, logs, summary report>`
## 12. Remaining test tasks
`<open items + owners>`
## 13. Environmental needs
`<hw/sw/data/test-data reset>`
## 14. Staffing and training needs
`<roles, training>`
## 15. Responsibilities
`<who owns what; RACI ref>`
## 16. Schedule
`<milestones, entry/exit dates>`
## 17. Planning risks and contingencies
`<risk → contingency>`
## 18. Approvals
`<name / role / date>`
## Glossary
`<terms>`
